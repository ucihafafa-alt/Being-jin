rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /bmi_submissions/{docId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
